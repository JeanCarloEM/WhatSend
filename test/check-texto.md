$diatarde$, ${nome}.

Esta mensagem e uma fixture de validacao do RCF.

Valor base: ${$.moeda(valor)}
Valor atualizado: ${$.moeda(valor+taxa)}
Conta formatada: ${$.digito2(conta)}

*Confirmacao:* este modelo existe apenas para `npm run check:test`.
